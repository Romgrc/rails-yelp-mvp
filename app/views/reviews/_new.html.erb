<div class="container">
  <div class="row mt-4">
    <div class="col">
      <div class="row">
        <div class="col-lg-8">
          <%= simple_form_for [restaurant, review] do |f| %>
            <%= f.input :content %>
            <%= f.input :rating %>

            <%= f.submit 'new review', class: 'btn btn-primary' %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
